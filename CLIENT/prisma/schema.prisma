generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model info {
  info_id            String    @id @default(uuid()) // Assuming UUIDs for info_id
  supabase_user_id   String
  public             Boolean   @default(true)
  video_name         String?
  video_description  String?
}